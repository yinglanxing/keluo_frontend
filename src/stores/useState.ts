// 全局状态
import { defineStore } from 'pinia';

export const useState = defineStore('state', {
    state(): GlobalState {
        
    },
    getters: {
        
    },
    actions: {
        
    },
});

interface GlobalState {
    
}
